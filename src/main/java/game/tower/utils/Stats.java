package game.tower.utils;

public record Stats(int rangeRadius,
                    int reloadTime,
                    int projectileDamage,
                    int projectileSpeed,
                    int upgradePrice,
                    int sellPrice) {
}
